あなたには、卒業論文で使用する「PWA家計簿アプリケーション」の実装支援を依頼します。

本開発では、AIにアプリケーション全体を一括生成させるのではなく、実装者である私が、各ファイルの役割、コードの処理内容、データの流れ及び採用理由を理解し、卒業論文で説明できる状態にすることを最も重視します。

完成速度だけを優先せず、必ず一工程ずつ進めてください。

---

# 1. 最初に確認する資料

リポジトリ内に、以下の資料があります。

* `README.md`
* `docs/specification.md`
* `docs/architecture.md`
* `docs/decisions.md`
* `docs/test-plan.md`
* `docs/research-context.md`

コードを作成する前に、必ずこれらをすべて確認してください。

各資料の役割は以下のとおりです。

* `README.md`

  * プロジェクト全体の概要
* `docs/specification.md`

  * 実装する機能、画面、データ及び動作仕様
* `docs/architecture.md`

  * ファイル構成、各ファイルの責務及び処理の流れ
* `docs/decisions.md`

  * 採用した設計判断とその理由
* `docs/test-plan.md`

  * 完成条件、テスト項目及び評価方法
* `docs/research-context.md`

  * 卒業論文の研究目的、背景及び設計思想

実装範囲については、主に`specification.md`及び`decisions.md`を基準としてください。

`research-context.md`は、実装対象を追加するための資料ではなく、仕様や設計の理由を理解するための補助資料です。

資料間に矛盾、不足又は解釈が分かれる箇所がある場合は、勝手に判断して実装せず、次の形式で報告してください。

* 該当する資料と箇所
* 矛盾又は不明点の内容
* 考えられる選択肢
* 各選択肢の利点と欠点
* 研究目的に照らした推奨案

私が判断するまでは、その箇所の実装を進めないでください。

既存のREADME及びdocsの内容は、明示的に依頼しない限り変更しないでください。

---

# 2. 開発するシステム

通信環境がない場合でも、支出・収入の記録、履歴閲覧、編集、削除及び集計を行えるPWA家計簿アプリケーションを開発します。

家計記録は外部サーバへ送信せず、IndexedDBを用いて利用者の端末内へ保存します。

本システムでは、金融機関連携やAI分析などの高度な機能ではなく、必要最低限の家計記録を、通信環境に左右されにくい状態で利用できることを重視します。

---

# 3. 使用する技術

以下の技術を使用します。

* HTML
* CSS
* JavaScript
* ES Modules
* IndexedDB
* Web App Manifest
* Service Worker
* Cache Storage
* 必要性を確認した場合のみStorage API

以下は使用しません。

* React
* Vue
* Angular
* その他のフロントエンドフレームワーク
* バックエンド
* クラウドデータベース
* 認証サービス
* UIコンポーネントライブラリ
* 必要性を説明できない外部ライブラリ

現段階では、Viteなどのビルドツールも使用しません。

HTML、CSS及びJavaScriptを、ローカルWebサーバーから直接読み込む構成とします。

---

# 4. 実装順序

以下の順序を正式な実装順序とします。

1. HTML・CSS・JavaScriptの初期構成
2. IndexedDBの接続
3. 家計記録の新規登録
4. 履歴表示
5. ページ再読み込み後の保持確認
6. 編集
7. 削除
8. ホーム画面の集計
9. 月別及びカテゴリ別集計
10. 画面適応
11. Web App Manifest
12. Service Worker及びキャッシュ
13. オフライン動作確認
14. 永続ストレージ要求の検討
15. 公開環境での動作確認

この順序を必ず守ってください。

現在指定されている工程より後の機能を、便利だからという理由で先回りして実装しないでください。

例えば、工程2「IndexedDBの接続」では、新規登録、履歴表示、編集、削除、集計及びService Workerを実装しないでください。

各工程の動作確認が完了し、私が次へ進むよう指示してから、次の工程を開始してください。

---

# 5. 現在の状態

現在のリポジトリには、以下のみが存在します。

* `README.md`
* `docs/`

アプリケーション本体のHTML、CSS及びJavaScriptは、まだ作成していません。

最初に進める工程は、次の工程です。

## 工程1

HTML・CSS・JavaScriptの初期構成

この工程では、IndexedDB、家計記録の保存、履歴表示、編集、削除、集計、Web App Manifest及びService Workerは実装しません。

---

# 6. 工程1の目的

工程1では、以下を実現してください。

* ローカルWebサーバーでアプリケーションを起動できる
* HTMLからCSSを読み込める
* HTMLからJavaScriptをES Modulesとして読み込める
* JavaScriptの初期化処理が実行される
* 家計記録入力画面の基本構造を表示できる
* 履歴を表示するための空の領域を用意する
* 当日の日付を入力欄の初期値として表示する
* JavaScriptが動作していることを確認できる
* ブラウザのコンソールにエラーが発生しない

この段階では、入力した家計記録を保存しないでください。

「記録する」ボタンを押した場合は、保存処理が未実装であることを確認できる仮メッセージを表示する程度にしてください。

---

# 7. UIに関する方針

UIの最終デザインは、まだ決定していません。

後の工程で、卒業論文に記載した以下の設計方針を基準として複数のUI案を比較し、採用案を決定します。

* 支出・収入の記録を短い操作で開始できる
* 画面上の情報量を増やしすぎない
* スマートフォンで操作しやすい
* 入力内容及び操作結果を理解しやすい
* ホーム、記録入力、履歴及び集計の役割を分ける
* 装飾性よりも操作性と視認性を優先する
* オンラインとオフラインで操作方法を変えない

そのため、工程1では完成されたデザインを作らないでください。

次の範囲にとどめてください。

* セマンティックなHTML構造
* アクセシビリティに配慮したラベル
* スマートフォンで大きく崩れない最低限のCSS
* 後からUIを変更しやすいクラス構造
* 過度な色、装飾、アニメーションを使用しない仮デザイン

UIを独自判断で最終決定しないでください。

---

# 8. コード生成時の必須ルール

## 8.1 一工程だけを実装する

今回の回答では工程1だけを扱ってください。

工程2以降のコードを含めないでください。

---

## 8.2 コードを省略しない

コードを提示する場合は、今回作成又は変更するファイルについて、全文を提示してください。

差分だけの提示や、次のような省略は使用しないでください。

* 「以下を追加」
* 「既存コードは省略」
* 「同様に実装」
* `...`

そのままファイルへ貼り付けて動作する状態で提示してください。

ただし、変更していないファイルを毎回再掲する必要はありません。

---

## 8.3 ファイルの責務を分ける

工程1では、最低限次の構成を基本としてください。

```text
index.html
css/
  style.css
js/
  main.js
```

各ファイルの責務は以下とします。

* `index.html`

  * 画面の意味的な構造
* `css/style.css`

  * 表示及び最低限の画面適応
* `js/main.js`

  * アプリケーションの初期化及び仮のイベント処理

HTML内へJavaScriptやCSSを直接記述しないでください。

---

## 8.4 不要な抽象化を行わない

将来の拡張を理由として、工程1の段階から複雑なクラス、継承、デザインパターン、状態管理機構又は独自フレームワークを導入しないでください。

処理の責務を分離しつつ、卒業論文で説明できる単純な構成を優先してください。

---

## 8.5 コメントの方針

コードへ過剰なコメントを追加しないでください。

次のような、コードを読めば分かる内容を一行ずつ説明するコメントは不要です。

```js
// ボタンを取得する
const button = document.querySelector("#button");
```

一方で、採用理由や注意点がコードだけでは分かりにくい処理には、簡潔なコメントを付けてください。

---

## 8.6 エラーを無視しない

DOM要素を取得できなかった場合など、想定外の状態を無視して処理を継続しないでください。

ただし、工程1の規模に対して過剰に複雑なエラー処理は導入しないでください。

---

## 8.7 アクセシビリティ

最低限、以下へ配慮してください。

* `label`と入力欄を関連付ける
* 見出しの階層を正しくする
* `fieldset`と`legend`を適切に使用する
* ボタンには役割の分かる文言を使用する
* 状態メッセージを支援技術でも確認できるようにする
* 色だけで収入と支出を区別しない構造にする
* キーボード操作を妨げない

---

# 9. 各工程で必要な説明

回答は、必ず以下の順番で構成してください。

## 9.1 資料確認結果

最初に、README及びdocsを確認した結果をまとめてください。

次を明示してください。

* 今回の工程に関係する仕様
* 今回は実装しない仕様
* 資料間の矛盾又は未確定事項の有無

資料を確認できていない場合は、確認したふりをせず、そのことを明示してください。

---

## 9.2 今回の実装範囲

工程1で実装する内容と、実装しない内容を明確に分けてください。

---

## 9.3 作成するファイルと役割

各ファイルについて、以下を説明してください。

* ファイルの役割
* 今回記述する処理
* 他のファイルとの関係
* その処理をそのファイルへ書く理由

---

## 9.4 処理の流れ

コードを提示する前に、ブラウザで次の処理がどの順番で実行されるのか説明してください。

例：

1. ブラウザが`index.html`を読み込む
2. CSSを読み込む
3. JavaScriptモジュールを読み込む
4. JavaScriptが必要なDOM要素を取得する
5. 当日の日付を設定する
6. フォーム送信時のイベントを登録する
7. ボタン操作時に仮メッセージを表示する

---

## 9.5 ファイル全文

今回作成又は変更する各ファイルのコード全文を提示してください。

---

## 9.6 コードの説明

コード提示後に、最低限以下を説明してください。

* 主要なHTML要素の役割
* CSSの主要なレイアウト方針
* JavaScriptの定数及び変数が保持するもの
* 各関数が何を行うか
* 各関数の引数と戻り値
* イベントがいつ発生するか
* 初期化処理の流れ
* エラー発生時の処理

一行ずつ文法を説明するのではなく、ファイル単位及び処理単位で説明してください。

---

## 9.7 動作確認方法

以下を含む具体的な確認手順を提示してください。

* 使用するローカルWebサーバーの起動方法
* ブラウザで開くURL
* 画面上で確認する内容
* 開発者ツールのコンソールで確認する内容
* 正常時の期待結果
* 問題があった場合に共有すべき情報

---

## 9.8 卒業論文用の実装メモ

工程終了時に、以下を簡潔にまとめてください。

* 今回実装した内容
* 採用した構成
* その構成を採用した理由
* 第5章「実装」に記載できる内容
* 実装時の問題として記録すべき事項
* 現段階で設計書と実装に差異があるか

この内容はチャット上へ提示してください。

明示的に依頼しない限り、新しいドキュメントファイルを勝手に作成しないでください。

---

# 10. 動作確認後の進行

工程1のコードと動作確認方法を提示した後は、そこで停止してください。

私が実行結果を報告するまで、工程2「IndexedDBの接続」へ進まないでください。

正常に動作した場合は、工程1の完了条件を確認してから次へ進みます。

問題が発生した場合は、最初に以下を整理してください。

* 発生した現象
* 本来の期待結果
* エラーメッセージ
* 原因として考えられる範囲
* 原因を切り分ける確認方法

原因が分からないまま複数の修正を同時に行わないでください。

---

以上の条件に従い、最初にREADME及びdocsを確認し、工程1「HTML・CSS・JavaScriptの初期構成」だけを進めてください。
